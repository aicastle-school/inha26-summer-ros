# ROS2 기초 교육 자료

## 문서 정보

| 항목 | 내용 |
|---|---|
| 교육 일시 | 2026년 06월 22일 (월) |
| 교육 목표 | ROS2에 대한 기초 개념 학습 |
| 교육 자료 PPT | [교육 자료 PPT](https://docs.google.com/presentation/d/1jIMftRxqvUlGgqH2I7J55W5nW-wACVVp/edit?usp=sharing&ouid=108372010918579280832&rtpof=true&sd=true) |
| 교육 실습 | [PhysiCar for Codespaces](https://github.com/physicar-ai/physicar-for-codespaces) |

- 깃허브 계정 필요: [GitHub 가입 링크](https://github.com/signup)

---

## 1. 실습 환경 구성

## 1.1 PhysiCar for Codespaces 실습 시작

1. [**PhysiCar for Codespaces**](https://github.com/physicar-ai/physicar-for-codespaces) 접속
1. **Open PhysiCar Sim** 클릭
1. **Create new codespace** 클릭
1. 화면이 열리고 2분 후 `app.physicar`가 열리면 준비 완료

---

## 2. 피지카 ROS 2 주요 인터페이스

### 2.1 Topic 목록

| 이름 | 종류 | 타입 | 설명 |
|------|------|------|------|
| `/camera/image_raw/compressed` | topic | [`CompressedImage`](https://docs.ros2.org/latest/api/sensor_msgs/msg/CompressedImage.html) | 카메라 영상 (JPEG) |
| `/camera/pan` | topic | [`Float64`](https://docs.ros2.org/latest/api/std_msgs/msg/Float64.html) | 팬 (rad) |
| `/camera/tilt` | topic | [`Float64`](https://docs.ros2.org/latest/api/std_msgs/msg/Float64.html) | 틸트 (rad) |
| `/cmd_vel` | topic | [`Twist`](https://docs.ros2.org/latest/api/geometry_msgs/msg/Twist.html) | 속도 + 조향 |
| `/imu` | topic | [`Imu`](https://docs.ros2.org/latest/api/sensor_msgs/msg/Imu.html) | IMU (50 Hz) |
| `/odom` | topic | [`Odometry`](https://docs.ros2.org/latest/api/nav_msgs/msg/Odometry.html) | 오도메트리 |
| `/scan` | topic | [`LaserScan`](https://docs.ros2.org/latest/api/sensor_msgs/msg/LaserScan.html) | LiDAR 스캔 |

---

## 3. ROS2 워크스페이스 및 패키지 관리

### 3.1 워크스페이스란?

ROS2 워크스페이스는 여러 ROS2 패키지를 모아서 개발하고 빌드하는 작업 공간이다.

일반적으로 하나의 워크스페이스 안에는 다음과 같은 폴더가 있다.

```text
ros2_ws/
  ├── src/          # 소스 코드 디렉토리
  ├── build/        # 빌드 중간 결과물
  ├── install/      # 실행 가능한 설치 결과물
  └── log/          # 빌드 로그
```

## 3.2 워크스페이스 생성

### 3.2.1 워크스페이스 디렉토리 생성

```bash
mkdir -p ~/ros2_ws/src
cd ~/ros2_ws
```

### 3.2.2 워크스페이스 구조 확인

```bash
tree ~/ros2_ws/
```

---

## 3.3 패키지 생성

### 3.3.1 Python 기반 패키지 생성

```bash
cd ~/ros2_ws/src
ros2 pkg create --build-type ament_python my_first_package
```

### 3.3.2 C++ 기반 패키지 생성

```bash
cd ~/ros2_ws/src
ros2 pkg create --build-type ament_cmake my_cpp_package
```

### 3.3.3 의존성이 있는 패키지 생성

```bash
cd ~/ros2_ws/src
ros2 pkg create --build-type ament_python my_package --dependencies rclpy std_msgs
```

---

## 3.4 생성된 Python 패키지 구조

```text
my_first_package/
  ├── my_first_package/       # 패키지 소스 코드
  │   └── __init__.py
  ├── test/                   # 테스트 코드
  ├── package.xml             # 패키지 메타데이터
  ├── setup.py                # Python 설치 설정
  ├── setup.cfg               # 설정 파일
  └── resource/
```

---

## 3.5 package.xml 편집

```bash
cd ~/ros2_ws/src/my_first_package
nano package.xml
```

확인 및 수정할 주요 항목:

```xml
<name>my_first_package</name>
<version>0.0.0</version>
<description>ROS2 basic practice package</description>
<maintainer email="user@example.com">user</maintainer>
<license>Apache-2.0</license>

<depend>rclpy</depend>
<depend>std_msgs</depend>
```

---

## 3.6 패키지 빌드

### 3.6.1 워크스페이스로 이동

```bash
cd ~/ros2_ws
```

### 3.6.2 전체 워크스페이스 빌드

```bash
colcon build
```

### 3.6.3 특정 패키지만 빌드

```bash
colcon build --packages-select my_first_package
```

### 3.6.4 symlink-install 옵션 사용

```bash
colcon build --packages-select my_first_package --symlink-install
```

Python 패키지를 개발할 때는 `--symlink-install` 옵션을 사용하면 코드 수정 후 반영이 편하다.

---

## 3.7 빌드 결과 확인

```bash
ls -la ~/ros2_ws/build/my_first_package/
ls -la ~/ros2_ws/install/my_first_package/
```

---

## 3.8 환경 설정 및 source

### 3.8.1 워크스페이스 환경 로드

```bash
source ~/ros2_ws/install/setup.bash
```

### 3.8.2 개발 중 local setup 적용

```bash
source ~/ros2_ws/install/local_setup.bash
```

### 3.8.3 새 터미널마다 자동 적용하기

```bash
echo "source ~/ros2_ws/install/setup.bash" >> ~/.bashrc
source ~/.bashrc
```

zsh를 사용하는 경우:

```bash
echo "source ~/ros2_ws/install/setup.zsh" >> ~/.zshrc
source ~/.zshrc
```

---

## 3.9 환경 확인

```bash
echo $ROS_PACKAGE_PATH
ros2 pkg list | grep my_first_package
ros2 pkg prefix my_first_package
```

---

## 3.10 노드 실행

```bash
ros2 run my_first_package node_name
```

예시:

```bash
ros2 run my_first_package talker
```

---

## 3.11 launch 파일 실행

```bash
ros2 launch my_first_package launch_name.launch.py
ros2 launch my_first_package all_nodes.launch.py
```

---

## 3.12 실행 중인 ROS2 구성 확인

```bash
ros2 node list
ros2 topic list
ros2 node info /my_node
```

---

## 3.13 개발 워크플로우 정리

```bash
# 1. 워크스페이스 디렉토리로 이동
cd ~/ros2_ws

# 2. 코드 작성 또는 수정
# src/my_first_package/my_first_package/ 디렉토리의 Python 파일 수정

# 3. 빌드
colcon build --packages-select my_first_package

# 4. 환경 설정
source install/setup.bash

# 5. 노드 실행
ros2 run my_first_package node_name

# 6. 다른 터미널에서 모니터링
ros2 topic echo /topic_name
```

---

# 4. 실습 명령어 모음

## 4.1 노드 관련 명령어

```bash
ros2 node list
ros2 node info <node_name>
```

---

## 4.2 토픽 관련 명령어

```bash
ros2 topic list
ros2 topic info <topic_name>
ros2 topic echo <topic_name>
ros2 topic pub <topic_name> <msg_type> "<msg_content>"
ros2 topic hz <topic_name>
ros2 topic bw <topic_name>
```

---

## 4.3 카메라 제어

### 4.3.1 카메라 영상 확인

```bash
ros2 topic echo /camera/image_raw/compressed
```

### 4.3.2 팬 제어

```bash
ros2 topic pub /camera/pan std_msgs/msg/Float64 "{data: 0.5}"
```

### 4.3.3 틸트 제어

```bash
ros2 topic pub /camera/tilt std_msgs/msg/Float64 "{data: 0.3}"
```

---

## 4.4 속도 제어

### 4.4.1 전진

```bash
ros2 topic pub /cmd_vel geometry_msgs/msg/Twist "{linear: {x: 0.5, y: 0, z: 0}, angular: {x: 0, y: 0, z: 0}}"
```

### 4.4.2 회전

```bash
ros2 topic pub /cmd_vel geometry_msgs/msg/Twist "{linear: {x: 0, y: 0, z: 0}, angular: {x: 0, y: 0, z: 0.5}}"
```

### 4.4.3 정지

```bash
ros2 topic pub /cmd_vel geometry_msgs/msg/Twist "{linear: {x: 0, y: 0, z: 0}, angular: {x: 0, y: 0, z: 0}}"
```

---

## 4.5 센서 정보 수신

```bash
ros2 topic echo /imu
ros2 topic echo /odom
ros2 topic echo /scan
```

---

## 4.6 서비스 및 파라미터

### 4.6.1 서비스

```bash
ros2 service list
ros2 service type <service_name>
ros2 service call <service_name> <service_type> "<request>"
```

### 4.6.2 파라미터

```bash
ros2 param list
ros2 param get <node_name> <param_name>
ros2 param set <node_name> <param_name> <value>
```

---

## 4.7 ROS2 Bag 녹화 및 재생

```bash
ros2 bag record -a
ros2 bag record <topic_name1> <topic_name2>
ros2 bag play <bag_file>
ros2 bag info <bag_file>
```

---

## 4.8 launch 실행

```bash
ros2 launch <package_name> <launch_file>
ros2 launch <package_name> <launch_file> --show-args
```

---

# 5. 실습 예제 코드

## 5.1 기본 노드 생성

### 5.1.1 파일 생성

```bash
cd ~/ros2_ws/src/my_first_package/my_first_package
nano basic_node.py
```

### 5.1.2 기본 노드 코드

```python
import rclpy
from rclpy.node import Node

class MyBasicNode(Node):
    def __init__(self):
        super().__init__('my_basic_node')
        self.get_logger().info('ROS 2 기본 노드가 성공적으로 실행되었습니다!')

def main(args=None):
    rclpy.init(args=args)
    node = MyBasicNode()

    try:
        rclpy.spin(node)
    except KeyboardInterrupt:
        node.get_logger().info('사용자에 의해 노드가 종료되었습니다.')
    finally:
        node.destroy_node()
        rclpy.shutdown()

if __name__ == '__main__':
    main()
```

### 5.1.3 setup.py 엔트리 포인트 추가

```python
entry_points={
    'console_scripts': [
        'basic_node = my_first_package.basic_node:main',
    ],
},
```

### 5.1.4 빌드 및 실행

```bash
cd ~/ros2_ws
colcon build --packages-select my_first_package
source install/setup.bash
ros2 run my_first_package basic_node
```

### 5.1.5 예상 결과

```text
[INFO] [my_basic_node]: ROS 2 기본 노드가 성공적으로 실행되었습니다!
```

---

## 5.2 미니멀 퍼블리셔

### 5.2.1 파일 생성

```bash
cd ~/ros2_ws/src/my_first_package/my_first_package
nano minimal_publisher.py
```

### 5.2.2 코드

```python
import rclpy
from rclpy.node import Node
from std_msgs.msg import String

class MinimalPublisher(Node):
    def __init__(self):
        super().__init__('minimal_publisher')
        self.publisher_ = self.create_publisher(String, 'topic', 10)
        timer_period = 0.5  # seconds
        self.timer = self.create_timer(timer_period, self.timer_callback)
        self.i = 0

    def timer_callback(self):
        msg = String()
        msg.data = 'Hello World: %d' % self.i
        self.publisher_.publish(msg)
        self.get_logger().info('Publishing: "%s"' % msg.data)
        self.i += 1

def main(args=None):
    rclpy.init(args=args)
    minimal_publisher = MinimalPublisher()
    rclpy.spin(minimal_publisher)
    minimal_publisher.destroy_node()
    rclpy.shutdown()

if __name__ == '__main__':
    main()
```

---

## 5.3 미니멀 서브스크라이버

### 5.3.1 파일 생성

```bash
cd ~/ros2_ws/src/my_first_package/my_first_package
nano minimal_subscriber.py
```

### 5.3.2 코드

```python
import rclpy
from rclpy.node import Node
from std_msgs.msg import String

class MinimalSubscriber(Node):
    def __init__(self):
        super().__init__('minimal_subscriber')
        self.subscription = self.create_subscription(
            String,
            'topic',
            self.listener_callback,
            10)
        self.subscription

    def listener_callback(self, msg):
        self.get_logger().info('I heard: "%s"' % msg.data)

def main(args=None):
    rclpy.init(args=args)
    minimal_subscriber = MinimalSubscriber()
    rclpy.spin(minimal_subscriber)
    minimal_subscriber.destroy_node()
    rclpy.shutdown()

if __name__ == '__main__':
    main()
```

---

## 5.4 setup.py 엔트리 포인트 전체 예시

```python
entry_points={
    'console_scripts': [
        'basic_node = my_first_package.basic_node:main',
        'talker = my_first_package.minimal_publisher:main',
        'listener = my_first_package.minimal_subscriber:main',
    ],
},
```

---

## 5.5 PhysiCar 간단한 주행 코드

### 5.5.1 파일 생성

```bash
cd ~/ros2_ws/src/my_first_package/my_first_package
nano drive_controller.py
```

### 5.5.2 코드

```python
import rclpy
from rclpy.node import Node
from geometry_msgs.msg import Twist
import time

class PhysiCarDriveController(Node):
    def __init__(self):
        super().__init__('drive_controller')
        self.cmd_vel_pub = self.create_publisher(Twist, '/cmd_vel', 10)
        self.get_logger().info('PhysiCar Drive Controller initialized')

    def move_forward(self, speed=0.5, duration=3.0):
        msg = Twist()
        msg.linear.x = float(speed)

        start_time = time.time()
        self.get_logger().info(f'Moving forward: speed={speed}')
        while time.time() - start_time < duration:
            self.cmd_vel_pub.publish(msg)
            time.sleep(0.1)

    def move_backward(self, speed=0.3, duration=2.0):
        msg = Twist()
        msg.linear.x = -float(speed)

        start_time = time.time()
        self.get_logger().info(f'Moving backward: speed={speed}')
        while time.time() - start_time < duration:
            self.cmd_vel_pub.publish(msg)
            time.sleep(0.1)

    def stop(self):
        msg = Twist()
        msg.linear.x = 0.0
        msg.angular.z = 0.0
        self.cmd_vel_pub.publish(msg)
        self.get_logger().info('Stopped')
        time.sleep(0.2)

def main(args=None):
    rclpy.init(args=args)
    controller = PhysiCarDriveController()

    try:
        controller.move_forward(speed=0.5, duration=3.0)
        time.sleep(1.0)

        controller.move_backward(speed=0.3, duration=2.0)
        time.sleep(1.0)

        controller.stop()

        controller.get_logger().info('Drive sequence completed')
    except KeyboardInterrupt:
        controller.get_logger().info('Interrupted by user')
    finally:
        controller.destroy_node()
        rclpy.shutdown()

if __name__ == '__main__':
    main()
```

---

## 5.6 PhysiCar 카메라 데이터 수신

### 5.6.1 파일 생성

```bash
cd ~/ros2_ws/src/my_first_package/my_first_package
nano camera_viewer.py
```

### 5.6.2 코드

```python
import rclpy
from rclpy.node import Node
from sensor_msgs.msg import CompressedImage
import cv2
import numpy as np

class CameraViewer(Node):
    def __init__(self):
        super().__init__('camera_viewer')
        self.subscription = self.create_subscription(
            CompressedImage,
            '/camera/image_raw/compressed',
            self.image_callback,
            10)
        self.frame_count = 0
        self.get_logger().info('Camera Viewer Node started. Window will appear shortly.')

    def image_callback(self, msg):
        self.frame_count += 1

        try:
            np_arr = np.frombuffer(msg.data, np.uint8)
            cv_image = cv2.imdecode(np_arr, cv2.IMREAD_COLOR)

            if self.frame_count % 100 == 0:
                height, width = cv_image.shape[:2]
                self.get_logger().info(f'Frame {self.frame_count}: {width}x{height}')

            cv2.imshow("PhysiCar Camera View", cv_image)
            cv2.waitKey(1)

        except Exception as e:
            self.get_logger().error(f'Error processing image: {e}')

    def destroy_node(self):
        cv2.destroyAllWindows()
        super().destroy_node()

def main(args=None):
    rclpy.init(args=args)
    camera_viewer = CameraViewer()

    try:
        rclpy.spin(camera_viewer)
    except KeyboardInterrupt:
        camera_viewer.get_logger().info('Node stopped by user')
    finally:
        camera_viewer.destroy_node()
        rclpy.shutdown()

if __name__ == '__main__':
    main()
```

### 5.6.3 의존성 설치

Jazzy 환경이면 다음처럼 설치한다.

```bash
sudo apt-get install ros-jazzy-cv-bridge python3-opencv
```

Humble 환경이면 다음처럼 설치한다.

```bash
sudo apt-get install ros-humble-cv-bridge python3-opencv
```

---

# 6. rqt_image_view로 카메라 영상 보기

## 6.1 rqt_image_view 설치

Jazzy 환경:

```bash
sudo apt-get install ros-jazzy-rqt-image-view
```

Humble 환경:

```bash
sudo apt-get install ros-humble-rqt-image-view
```

---

## 6.2 rqt_image_view 실행

```bash
rqt_image_view
```

GUI 창에서 Topic 드롭다운을 열고 다음 토픽을 선택한다.

```text
/camera/image_raw/compressed
```

---

## 6.3 실시간 모니터링

터미널 1:

```bash
rqt_image_view
```

터미널 2:

```bash
ros2 run my_first_package drive
```

---

## 6.4 명령어로 카메라 확인

```bash
ros2 topic info /camera/image_raw/compressed
ros2 topic echo /camera/image_raw/compressed --once
ros2 topic hz /camera/image_raw/compressed
```

---

# 7. 문제 해결

## 7.1 워크스페이스 관련 문제

### 7.1.1 패키지를 찾을 수 없음

```bash
source ~/ros2_ws/install/setup.bash
ros2 pkg list | grep my_first_package
```

또는 전체 재빌드:

```bash
cd ~/ros2_ws
rm -rf build install log
colcon build
source install/setup.bash
```

---

### 7.1.2 새 터미널에서 환경 설정이 적용되지 않음

```bash
source ~/ros2_ws/install/setup.bash
```

자동 적용:

```bash
echo "source ~/ros2_ws/install/setup.bash" >> ~/.bashrc
source ~/.bashrc
```

---

### 7.1.3 의존성 에러

```bash
rosdep install --from-paths src --ignore-src -r -y
```

또는 `package.xml`에 의존성 추가:

```xml
<depend>std_msgs</depend>
```

---

## 7.2 빌드 관련 문제

### 7.2.1 Python 경로 오류

```bash
colcon build --symlink-install
source install/local_setup.bash
```

---

### 7.2.2 빌드 실패

```bash
colcon build --packages-select my_first_package --event-handlers console_direct+
cat log/latest_build/my_first_package/stdout.log
```

---

## 7.3 실행 관련 문제

### 7.3.1 실행 파일을 찾을 수 없음

오류 예시:

```text
Could not find executable 'talker'
```

해결:

```bash
colcon build --packages-select my_first_package
source install/setup.bash
ros2 pkg executables my_first_package
```

`setup.py`의 `entry_points`에 실행 이름이 등록되어 있는지도 확인한다.

---

### 7.3.2 노드는 실행되지만 토픽 발행이 안 됨

```bash
ros2 node list
ros2 topic list
ros2 topic echo /topic
ros2 node info /my_node
```

---

### 7.3.3 센서 데이터가 안 받아짐

```bash
ros2 topic hz /camera/image_raw/compressed
ros2 topic echo /scan --once
```

시뮬레이터가 정상 실행 중인지 먼저 확인한다.

---

## 7.4 유용한 디버깅 명령어

```bash
ros2 topic list
ros2 node list
ros2 service list
ros2 --version
printenv | grep ROS
rqt_graph
ros2 interface show std_msgs/msg/String
ros2 interface show geometry_msgs/msg/Twist
```

---

# 8. 추가 과제

## 8.1 과제 1: 기본 노드 실행

### 목표

- ROS2 노드의 기본 구조 이해
- `rclpy.init()`, `Node`, `rclpy.spin()`, `shutdown()` 흐름 이해

### 실행

```bash
cd ~/ros2_ws
source install/setup.bash
ros2 run my_first_package basic_node
```

### 예상 결과

```text
[INFO] [my_basic_node]: ROS 2 기본 노드가 성공적으로 실행되었습니다!
```

---

## 8.2 과제 2: 미니멀 Pub/Sub 실행

### 목표

- 퍼블리셔와 서브스크라이버의 기본 동작 이해

### 실행

터미널 1:

```bash
cd ~/ros2_ws
source install/setup.bash
ros2 run my_first_package talker
```

터미널 2:

```bash
source ~/ros2_ws/install/setup.bash
ros2 run my_first_package listener
```

### 예상 결과

```text
[INFO] Publishing: "Hello World: 0"
[INFO] Publishing: "Hello World: 1"
[INFO] I heard: "Hello World: 0"
[INFO] I heard: "Hello World: 1"
```

---

## 8.3 과제 3: PhysiCar 간단한 주행

### 목표

- 로봇의 속도 제어 이해
- 전진/후진 명령 발행

### 파일 추가

```bash
cd ~/ros2_ws/src/my_first_package/my_first_package
nano drive_controller.py
```

### setup.py 수정

```python
entry_points={
    'console_scripts': [
        'basic_node = my_first_package.basic_node:main',
        'talker = my_first_package.minimal_publisher:main',
        'listener = my_first_package.minimal_subscriber:main',
        'drive = my_first_package.drive_controller:main',
    ],
},
```

### 빌드 및 실행

```bash
cd ~/ros2_ws
colcon build --packages-select my_first_package
source install/setup.bash
ros2 run my_first_package drive
```

### 예상 결과

```text
[INFO] PhysiCar Drive Controller initialized
[INFO] Moving forward: speed=0.5
[INFO] Moving backward: speed=0.3
[INFO] Stopped
[INFO] Drive sequence completed
```

---

## 8.4 과제 4: 카메라 데이터 수신 및 저장

### 목표

- 카메라 데이터 수신
- `CompressedImage` 처리
- OpenCV로 이미지 확인

### 파일 추가

```bash
cd ~/ros2_ws/src/my_first_package/my_first_package
nano camera_viewer.py
```

### package.xml 수정

```xml
<depend>sensor_msgs</depend>
<depend>cv_bridge</depend>
```

### setup.py 수정

```python
entry_points={
    'console_scripts': [
        'basic_node = my_first_package.basic_node:main',
        'talker = my_first_package.minimal_publisher:main',
        'listener = my_first_package.minimal_subscriber:main',
        'drive = my_first_package.drive_controller:main',
        'camera_viewer = my_first_package.camera_viewer:main',
    ],
},
```

### 빌드 및 실행

```bash
rosdep install --from-paths src --ignore-src -r -y
pip install opencv-python

cd ~/ros2_ws
colcon build --packages-select my_first_package
source install/setup.bash

ros2 run my_first_package camera_viewer
```

---

## 8.5 과제 5: rqt_image_view로 실시간 카메라 영상 보기

### 목표

- 실시간 카메라 영상 시각화

### 실행

터미널 1:

```bash
rqt_image_view
```

터미널 2:

```bash
ros2 run my_first_package drive
```

Topic 드롭다운에서 다음 토픽을 선택한다.

```text
/camera/image_raw/compressed
```

---

## 8.6 과제 6: 통합 실습

### 목표

- 주행과 카메라 데이터를 동시에 수집
- 여러 ROS2 노드가 동시에 동작하는 구조 이해

### 실행

터미널 1:

```bash
rqt_image_view
```

터미널 2:

```bash
ros2 run my_first_package camera_viewer
```

터미널 3:

```bash
ros2 run my_first_package drive
```

### 예상 결과

- 로봇이 전진/후진하면서 카메라 영상 변화
- 카메라 영상이 실시간으로 표시됨
- 각 노드가 독립적으로 실행되어 동시에 작동

---

# 9. 주요 학습 포인트

## 9.1 ROS2의 기본 구성요소

### 9.1.1 노드

노드는 ROS2의 기본 실행 단위이다.

- 독립적으로 실행되는 프로세스
- 다른 노드와 통신 가능
- 센서 처리, 제어, 인식, 판단 등을 각각의 노드로 나눌 수 있음

---

### 9.1.2 토픽

토픽은 노드 간 비동기 통신 채널이다.

- 퍼블리셔: 데이터 발행
- 서브스크라이버: 데이터 수신
- 1:N, N:1, N:N 통신 가능

비유하면 토픽은 여러 사람이 볼 수 있는 설문지 또는 게시판과 비슷하다.

---

### 9.1.3 서비스

서비스는 노드 간 동기 요청-응답 통신 방식이다.

- 클라이언트: 요청
- 서버: 응답
- 결과를 기다려야 하는 명령에 적합

---

### 9.1.4 파라미터

파라미터는 노드의 런타임 설정값이다.

- 속도 제한
- 센서 옵션
- 알고리즘 설정값
- 노드 실행 중 일부 값 변경 가능

---

## 9.2 PhysiCar 시뮬레이터 활용

- Codespaces 환경에서 브라우저 기반 ROS2 개발
- 카메라, LiDAR, IMU 등 다양한 센서 데이터 수신
- 로봇의 속도 및 조향 제어 실습
- 카메라 팬/틸트 제어 실습

---

## 9.3 실습 추천 흐름

1. 기본 개념 이해: 노드와 토픽 확인
2. 센서 데이터 수신: 카메라, LiDAR, IMU 확인
3. 로봇 제어: 속도 명령 발행
4. 능동 센싱: 카메라 팬/틸트 제어
5. 데이터 분석: Bag 파일 녹화 및 재생

---

## 9.4 실습 체크리스트

- [ ] 워크스페이스 생성 및 구조 이해
- [ ] 첫 번째 패키지 생성 및 빌드
- [ ] 환경 설정 적용
- [ ] 노드와 토픽 확인 명령어 실습
- [ ] 기본 노드 작성 및 실행
- [ ] 간단한 퍼블리셔/서브스크라이버 작성 및 실행
- [ ] PhysiCar 카메라 제어
- [ ] PhysiCar 속도 제어
- [ ] 센서 데이터 수신 및 모니터링
- [ ] 데이터 녹화 및 재생

---

# 10. 참고 자료

- [ROS2 공식 문서](https://docs.ros.org/en/humble/)
- [ROS2 튜토리얼](https://docs.ros.org/en/humble/Tutorials.html)
